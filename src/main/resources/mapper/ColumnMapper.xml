<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.content.mapper.ColumnMapper">
    
    <select id="findByCreatorId" resultType="com.platform.content.entity.PlatformColumn">
        SELECT * FROM platform_columns WHERE creator_id = #{creatorId} ORDER BY created_at DESC
    </select>
    
    <select id="findByCategoryId" resultType="com.platform.content.entity.PlatformColumn">
        SELECT * FROM platform_columns WHERE category_id = #{categoryId} AND status = 1 ORDER BY created_at DESC
    </select>
    
    <update id="incrementViewCount">
        UPDATE platform_columns SET view_count = view_count + 1 WHERE id = #{id}
    </update>
    
    <update id="incrementSubscribeCount">
        UPDATE platform_columns SET subscribe_count = subscribe_count + 1 WHERE id = #{id}
    </update>
    
</mapper>