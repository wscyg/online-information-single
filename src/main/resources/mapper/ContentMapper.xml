<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.content.mapper.ContentMapper">

    <update id="incrementViewCount">
        UPDATE contents
        SET view_count = view_count + 1
        WHERE id = #{id}
    </update>

    <select id="findByColumnId" resultType="com.platform.content.entity.Content">
        SELECT * FROM contents WHERE column_id = #{columnId} AND status = 1 ORDER BY sort_order ASC, created_at ASC
    </select>

</mapper>