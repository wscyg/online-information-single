<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.order.mapper.OrderMapper">
    
    <select id="findByOrderNo" resultType="com.platform.order.entity.Order">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>
    
    <select id="findByUserId" resultType="com.platform.order.entity.Order">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>
    
    <select id="findByUserIdAndStatus" resultType="com.platform.order.entity.Order">
        SELECT * FROM orders WHERE user_id = #{userId} AND status = #{status} ORDER BY created_at DESC
    </select>
    
</mapper>