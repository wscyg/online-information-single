<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.user.mapper.UserMapper">
    
    <select id="findByUsername" resultType="com.platform.common.entity.User">
        SELECT * FROM users WHERE username = #{username} AND status = 1
    </select>
    
    <select id="findByEmail" resultType="com.platform.common.entity.User">
        SELECT * FROM users WHERE email = #{email} AND status = 1
    </select>
    
    <select id="findByPhone" resultType="com.platform.common.entity.User">
        SELECT * FROM users WHERE phone = #{phone} AND status = 1
    </select>
    
</mapper>